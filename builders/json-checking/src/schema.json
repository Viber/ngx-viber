{
  "id": "JsonCheckingBuilderSchema",
  "title": "Json files checking builder",
  "description": "Json files checking builder",
  "properties": {
    "checkList": {
      "type": "array",
      "description": "List of the paths to the json files or directories for checking",
      "default": []
    },
    "removeBom": {
      "type": "boolean",
      "description": "Removes BOM",
      "default": true
    }
  }
}