import { InjectionToken } from '@angular/core';

export const VBR_WINDOW = new InjectionToken<Window>('VBR_WINDOW');
export const VBR_STORAGE_PREFIX = new InjectionToken<string>('VBR_STORAGE_PREFIX');
